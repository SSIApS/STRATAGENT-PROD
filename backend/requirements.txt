# STRATAGENT backend dependencies
# Last audited: 2026-06-05
# Block 5: migrated from google-generativeai to google-genai

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Google AI + Cloud
google-genai>=2.8.0
google-cloud-firestore>=2.16.0

# HTTP client (used by STRATALYST deep scan crawler)
httpx>=0.27.0

# Data validation
pydantic>=2.0.0

# PDF extraction
pypdf>=4.0.0

# Environment config
python-dotenv>=1.0.0

# Image processing (auto-resize large uploads before Firestore storage)
Pillow>=10.0.0

# Word document export (FI output brief download)
python-docx>=1.1.0
